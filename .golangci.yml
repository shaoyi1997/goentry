run:
  skip-dirs:
    - docs

linters-settings:
  maligned:
    suggest-new: true
  gocritic:
    disabled-checks:
      - singleCaseSwitch
  wsl:
    allow-cuddle-declarations: true

linters:
  disable:
    - gochecknoglobals
    - funlen
  enable-all: true